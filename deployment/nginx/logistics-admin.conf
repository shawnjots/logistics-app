server {
    listen 443;
    server_name logistics-admin.clensonic.co.zw;

    root /var/www/logistics-admin/wwwroot;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
		add_header Blazor-Environment "Production";
    }
}
